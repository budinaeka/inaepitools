# Jalankan skrip ini untuk memulai aplikasi
# Jika ini adalah pertama kali, pastikan Anda telah menjalankan install_packages.R terlebih dahulu

library(shiny)

message("Memulai aplikasi InaEpiTools...")
runApp("app.R", launch.browser = TRUE)
